# Scripts

This folder contains Bash scripts used in the Linux system administration lab.

## backup_system.sh

Creates a timestamped compressed backup of selected system directories and retains only the latest 5 backup archives.

## sync_data.sh

Synchronises files between client and server using rsync over SSH.

## Note

Update hostnames, paths, and usernames before running these scripts in a different environment.
